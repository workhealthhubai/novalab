import { PATHS } from '@/app/router/navigation';
import { ModulePlaceholder } from '../module-placeholder';

export function CompaniesPage() {
  return <ModulePlaceholder path={PATHS.companies} />;
}
